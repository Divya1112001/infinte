@model Assignmentmvc1.Models.Movie

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Moviename)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateofRelease)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model1)
    {
        <tr>
            <td>
                @Html.DisplayFor(MovieItem => item.Moviename)
            </td>
            <td>
                @Html.DisplayFor(MovieItem => item.DateofRelease)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ShipperID }) |
                @Html.ActionLink("Details", "Details", new { id = item.ShipperID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.ShipperID })
            </td>
        </tr>
    }

</table>
